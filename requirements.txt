# Vercel deployment requirements (backend API only - minimal dependencies)
# For serverless API endpoints that generate demo reviews

# HTTP Client
httpx==0.27.2

# Utilities
loguru==0.7.2
pydantic==2.9.2
pydantic-settings==2.6.0
